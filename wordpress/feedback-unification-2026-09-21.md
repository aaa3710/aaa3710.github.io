# 全8アプリのフィードバック入口統一 — 2026-09-21

**WordPress原本・登録入力案・ローカル配信候補へ反映済み。GitHub Pagesへのpush・一般公開は未実施。** 本人意思は管理のOWNER_INTENT、要求照合は[管理監査](../../publishing/audits/2026-09-21-feedback-url-consistency.md)を参照する。

全8アプリの日英を紹介／フィードバック／プライバシーの3タブへ統一。正規入口とASCのサポートURLは既存の `/apps/feedback/<slug>/`、英語は `/apps/en/feedback/<slug>/`。審査提出済みの伝わる文字URLを保持する。旧Supportは同じ言語のFeedbackへの直接転送で保持し、root直下の既存aliasも途中ページを挟まない。WordPressでは301、静的配信では即時HTML転送・手動リンク・noindex/nofollowとなる。

## 保存と既存内容の保全

131オブジェクトの現状を読み、64ページの最小差分を保存・読み戻し。本文42件、metadata32件（一部重複）。紹介本文とその料金・製品説明は変えず、既存のSupport本文16件を復元用にそのまま保持。専用16フォームとContact2フォームの受付optionは変更なし。対象外オブジェクトも全件不変。

撮影のものさしだけ欠けていた送信前の案内を、同じ受付方式の既存原稿に合わせて補った。TestFlight固有の案内は保持。他7本も共通Contactへの案内を同じ位置へ揃え、重複したページ下部リンクを除いた。旧Supportにある使い方・注意点は任意で開く補足として同じFeedbackページへ保持し、独立した二つの窓口を残さない。Privacy・紹介・Contactからの内部リンクも正規入口へ直接接続する。

非公開の編集前後原本・差分は `work/wordpress/feedback-unification-2026-09-21/`。各ページのcontent/excerpt hash・modified・metadata・編集ロックを保存直前に比較し、保存後に全文を読み戻した。最初のmetadata比較はWPの全meta読取と単キー読取の形式差で保存前に停止し、同じ形式で比較するよう直して実施した。

## 検証

- 公開中の既存URL62件（8アプリの日英3入力値と追加のFeedback14件）はHTTP 200。これは変更前の公開到達証拠であり、今回の統一本文・転送の配信証拠ではない。
- 原本由来の候補 `work/wordpress/exports/2026-09-21T05-38-25Z` は68/68必須ページ・113経路・226 HTML・3280検査に成功。
- 原本を新しく再書き出し、候補と242配信ファイルの一致を確認後、`site-output/` へローカル採用。buildと統一モードのverify成功。
- Web回帰53件成功。旧Support/既存root aliasの直接転送試験を全8アプリの日英へ拡張。フォームの重複・用途混線・不存在・別言語・転送ループ等の拒否を維持。
- 新規のWordPress書き出しは必ず `--feedback-unified` 検査を通す。全8組の統一、16ページそれぞれのフォーム1件・Privacy・Contact、3タブの順序と現在地を検査し、一部アプリだけ旧構造へ戻ることを防ぐ。過去の保存済みartifactの検証経路は保持。
- ブラウザで8アプリ×日英×390/1040pxの32表示を検査。横はみ出し0、3タブとFeedback現在地一致、専用フォーム各1件。撮影のものさし日英を目視し、補足の展開と本文Contactの下線を確認。Googleフォーム送信はしていない。
- 管理側で8入力案・日英16原稿・8候補版・保護原本2件を検査。入力欄の値とURL用途・slug・言語・出力ページを照合。

## 公開直前

既存方針は「修正依頼では原本保存まで、明示的な公開指示後に現在原本を公開」。今回の未push候補には、今回の統一だけでなく原本へ既に保存された道の記録の料金説明等も含む。公開指示時は現在の原本・この候補・remoteの差分を確認し、原本照合後に通常push、Actionsと実URLで配信を確認する。旧URLを削除しない。Forms設定・回答・Apple審査を変更しない。
