# 公開名・問い合わせ導線のローカル検証

2026-09-04。対象はこのサイトのローカル変更と生成HTML。GitHub Pages公開、実Googleフォームの改修・受付、アプリ実機・配布の証拠ではない。

## 結果

- 公開名を日英で更新し、紹介・Support・Privacy・Feedbackのslugを `focus-exposure-calculator` に統一。旧slugと旧共通Feedbackは生成物に存在せず、リダイレクトなし。
- 共通Contactとアプリ専用Feedbackを分離。Contactからの不具合・要望はSupportを経由する。専用Feedbackはサイトマップから除外し、`noindex, nofollow`。
- 形式検査、lint、設定テスト6/6が成功。
- 生成HTML検証は、受付有効のローカル試験と無効の試験でそれぞれ438/438成功。対象12ページ、公開28ルート、HTML56ファイル、サイトマップ10URLを照合。実フォームには入力・送信していない。
- 最終生成物は両受付フラグ未設定（無効）、公開サポートメール未設定。埋め込み、外部フォームリンク、仮メール、AI将来利用文は表示しない。
- 日英6種類のページを390px・1440pxの計24条件で確認。横はみ出し・画像破損なし、言語名表示あり。Contact → Support → Feedbackの実クリックも確認。
- Support・Contact・Feedbackの計12画面を独立目視。日本語Contact見出しの語中改行1件を修正し、2幅の再撮影・独立確認で解消。残る指摘なし。

## 画像と削除の扱い

- 旧名が写る実画面4枚はUIから非表示。アイコンを含む既存5ファイルは新ディレクトリへの移動前後でGit blob hashが一致し、無加工。
- 改名後の実画面は未作成。新しいmain／sensorsの日英実画面を撮影・照合するまで表示を戻さない。
- 共有用画像だけを組み込み画像生成の編集モードで日英2枚へ更新。既存の淡色・図表構成を参照し、JAは「ピントと光 — 撮影計算」「ピントの範囲を、見える形に。」、ENは「Focus & Light — Photo Tools」「See the focus range at a glance.」を指定。アプリ実画面の改字・偽装には使用していない。
- 共有用画像は `public/images/focus-exposure-calculator/og-ja.png` と `og-en.png`。1200×630 PNG、アルファなし、文字と構図を目視確認。
- 旧公開ルート、参照のない旧画面コンポーネント2件、旧名OG画像2件を削除。以前の内容はGit履歴から復元できる。
- ブラウザ確認画像はローカルの `output/playwright/` に保持し、Git対象外。

## 未実施と再開条件

外部ゲートの正本は [公開準備README](README.md#この変更の外部ゲート) と [公開前チェックリスト](release-checklist.md)。共通公開メールの採否、4フォームの実編集・設定照合と受付有効化、push後の公開URL確認を残す。改名後の実画面差し替えも公開前確認として残す。

追加同意チェックボックスは未採用。AI処理の実回答・実モデル・定期運用は未検証で、全環境の停止も保証されていない。本作業ではAI・schedulerを有効化していない。

push、公開、実フォーム変更、App Store操作、Apple実行は行っていない。

## 本番反映の再開準備（同日追記）

所有者によるサイト本番反映の許可を受領し、実行候補を `083a945` へ保存した。ブラウザ上のログインアカウントは既存所有者 `aaa3710`、公開先は `aaa3710/aaa3710.github.io`。本番`main`は `ded8357`、Actionsのrepository／environment variablesはいずれも未設定で、受付2用途は無効、公開メールも未設定。既存公開対象6コミットの独立読み取り監査で、秘密・フォーム編集URL・実回答・意図しない生成物の混入なし。保存済み生成HTMLの438項目も再照合して成功した。

`git push origin main`は自動安全審査で実行前拒否。元ユーザー発話を読み取り照合した後の同一操作の再審査も、当該サイトタスク内の直接承認が必要として拒否された（実行前拒否2件、実push開始0件、deploy開始0件）。迂回せず停止し、直接ユーザー入力または承認状態の変化を待つ。今回、追加build、フォーム操作、受付変数変更、Apple／App Store操作は行っていない。

既存正本に包含される事項として、証拠ゲートと担当境界をこの記録・PROJECT_CONTEXTへ反映した。共通思想の重複追記は行わない。

## Contactの返信境界（同日追記）

日英Contact・Privacy・Supportのsourceを主担当と独立担当が読み取り確認した。一般問い合わせの返信保証や、Web非掲載を根拠に返信時の送信元アドレス・表示名・Reply-Toも非開示とする保証はない。公開文面の変更は不要と判断し、受信確認と返信手段の確認を分けるようPROJECT_CONTEXTと公開前チェックリストだけを修正した。

公開メールは未設定、返信用送信元は未確定。実返信が必要な時は親タスクを通じた所有者の判断と別途の送信許可を待ち、新規メール作成を必須条件にしない。本追記で実メールの返信・試験送信・新規作成、フォーム操作、本番公開、受付変数変更は行っていない。source変更がないためbuild・画面検証は再実行していない。

管理担当からFeedback日英2件の安全設定と一時受付停止の途中経過を受領し、PROJECT_CONTEXTへ記録した。4件全体の準備完了ではないため、関連READMEとチェックリストの状態を「改修・設定照合は未完了」へ揃えた。受付有効化の根拠にはしていない。

横断学習は「特定project固有」へ分類し、このContactの運用境界を既存の正規文書へ記録した。共通思想やスキルの変更は行わない。
